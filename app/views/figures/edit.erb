<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Edit Figure</title>
  </head>
  <body>
    <h1>Edit Figure</h1>

    <form action="/figures/<%= @Figure.id %>" method="post">
      Name: <input type="text" name="figure[:name]" value="<%= @figure.name %>" id="name"><br><br>
      Titless:<br><br>
      <% @titles.each do |title| %>
        <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" <%= 'checked' if @figure.titles.include?(title)%>/>
        <%= title.name %><br>
        <br>
      <%end%>
      <br><br>

      Landmarks:<br><br>
      <% @landmarks.each do |landmark| %>
        <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark)%>/>
        <%= landmark.name %><br>
        <br>
      <%end%>


      <input type="hidden" name="_method" value="patch">
      <input type="submit" value="Edit Figure">
    </form>


  </body>
</html>
